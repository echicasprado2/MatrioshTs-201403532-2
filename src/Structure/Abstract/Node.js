class Node {
    /**
     * 
     * @param {*} line 
     * @param {*} column 
     */
    constructor(line, column) {
        this.line = line;
        this.column = column;
        this.translatedCode = "";
    }

    //TODO this metho is use for get code and add code in string
    getC3D(env,code){}

}
